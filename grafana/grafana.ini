# Only setting not already covered by a GF_* env var in docker-compose.yml
# (GF_INSTALL_PLUGINS downloads it; this allows Grafana to load it despite
# being unsigned — see AGENTS.md).
[plugins]
allow_loading_unsigned_plugins = motherduck-duckdb-datasource
